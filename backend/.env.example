# Server configuration
PORT=5000
NODE_ENV=development

# MongoDB connection
# For local MongoDB:
MONGO_URI=mongodb://localhost:27017/egybest
# For MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/egybest?retryWrites=true&w=majority

# JWT Secret for authentication (generate a strong random string)
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRE=30d

# Cookie settings
JWT_COOKIE_EXPIRE=30
